#include "bits/stdc++.h"
using namespace std;

int solution(vector<int> num_list) {
    int answer = 0;
    return answer;
}

int main(void) {
    cout << solution({4, 2, 6, 1, 7, 6}) << endl; // 17
    cout << solution({-1, 2, 5, 6, 3}) << endl; // 8
    return 0;
}
