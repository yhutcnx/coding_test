#include "bits/stdc++.h"
using namespace std;

vector<int> solution(vector<int> numbers) {
    vector<int> answer;
    return answer;
}

int main(void) {
    vector<int> res;

    res = solution({1, 2, 3, 4, 5});
    
    for(auto t:res){
        cout << t << ",";
    }
    cout << "\n"; // [2, 4, 6, 8, 10]

    return 0;
}