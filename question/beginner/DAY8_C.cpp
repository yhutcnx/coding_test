#include "bits/stdc++.h"
using namespace std;

vector<int> solution(vector<int> emergency) {
    vector<int> answer;
    return answer;
}

int main(void) {
    vector<int> res;

    res = solution({3, 76, 24});
    
    for(auto t:res){
        cout << t << ",";
    }
    cout << "\n"; // [3, 1, 2]

    return 0;
}