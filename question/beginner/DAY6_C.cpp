#include "bits/stdc++.h"
using namespace std;

vector<int> solution(vector<int> num_list) {
    vector<int> answer;
    return answer;
}

int main(void) {
    vector<int> res;

    res = solution({1, 2, 3, 4, 5});
    
    for(auto t:res){
        cout << t << ",";
    }
    cout << "\n"; // [2, 3]

    return 0;
}